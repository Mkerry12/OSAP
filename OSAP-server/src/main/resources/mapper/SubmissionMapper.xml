<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mqq.mapper.SubmissionMapper">

    <select id="pageMySubmitted" resultType="com.mqq.entity.SubmittedRecordRow">
        select sub.id as submission_id, sub.submit_at,
               s.id as survey_id, s.title, s.type, s.status, s.question_count, s.creator_id
        from submission sub
        join survey s on sub.survey_id = s.id
        where sub.user_id = #{userId}
        order by sub.submit_at desc
    </select>

    <select id="batchLatestSubmitAt" resultType="java.util.Map">
        select survey_id, max(submit_at) as latest_submit_at from submission
        where survey_id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        group by survey_id
    </select>

</mapper>
