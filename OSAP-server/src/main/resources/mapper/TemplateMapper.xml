<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mqq.mapper.TemplateMapper">

    <update id="update">
        update survey_template
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="category != null">category = #{category},</if>
            <if test="questions != null">questions = #{questions},</if>
            <if test="questionCount != null">question_count = #{questionCount},</if>
            update_at = #{updateAt}
        </set>
        where id = #{id}
    </update>

    <select id="pageQuery" resultType="com.mqq.entity.SurveyTemplate">
        select * from survey_template
        <where>
            <if test="category != null and category != ''">
                and category = #{category}
            </if>
        </where>
        order by create_at desc
    </select>

</mapper>
